/**
 * Server-side exports for other packages
 * This allows the extension and other apps to import the AppRouter type
 *
 * NOTE: Only import types from this file, not implementations
 */
export type { AppRouter } from './trpc/routers/_app'
